1,	lệnh báo lỗi
	sửa: bool isA = (90 <= grade && grade <=100);
2,	in ra 0
3,	vì khi nhân 2 số kiểu int với nhau sẽ tạo ra 1 số kiểu int
    số 4294967296 đã vượt quá phạm vi giá trị của kiểu dữ liệu integer
4,	biểu thức có giá trị false do không dùng dấu phẩy động
5,  chương trình không báo lỗi khi biên dịch, nhưng gặp lỗi khi chạy chương trình
6,	chương trình báo lỗi không cùng kiểu dữ liệu.
	  x là 1 giá trị rác, khi chuyển ra ngoài hàm main thì x có giá trị bằng 0
7,	đoạn code cho output lần lượt là: 0; 0.75; 0.75; 0.75
8,	chương trình báo lỗi do có 2 biến trừng tên
9,	bên trong khối lệnh con, định danh arg1 chỉ tới biến arg1 trong cùng khối lệnh
10,	khi ra khỏi khối lệnh con, định danh arg1 chỉ tới biến arg1 ngoài khối lệnh
11, 5/9 cho giá trị 0 nên C luôn cho giá trị bằng 0
12,	đoạn code không cho ra kết quả
